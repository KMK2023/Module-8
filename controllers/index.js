// The post controller is exported from this location. If there are many controllers, this will be one hub, where it is exported from.

module.exports={
    postController: require('./postController')
}